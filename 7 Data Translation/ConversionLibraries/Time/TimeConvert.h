//Works for both seconds and milliseconds
//Should literally just return whatever text from the .txt file that is passed in
namespace cvf {
    inline char* convertTime(char *line) {
        return line;
    }
}